const inputarea = document.getElementById('inputfield')
let len = inputarea.length

console.log(inputarea.length)

inputarea.addEventListener("input", () => {
    // const valuelen = inputarea.value
})